name: 'Bitcoind Test Action'
on:
  push:
    branches: [ "main", "dev" ]
    
  pull_request:
    branches: [ "main", "dev" ]
env:
  CARGO_TERM_COLOR: always

permissions:
  actions: read
  checks: write
  contents: read
  pull-requests: read

concurrency:
  group: '${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  test:
    uses: FairgateLabs/BitVMX-CI/.github/workflows/ci_local_template.yml@main
    with:
      repo: 'FairgateLabs/rust-bitvmx-workspace'
      submodule_path: 'rust-bitcoind'
      cargo_lock_path: 'rust-bitcoind/Cargo.lock'
      target_path: 'rust-bitcoind/target'
      single_thread_tests: true
    secrets: inherit